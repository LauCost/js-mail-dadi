//Chiede all'utente di inserire un numero da 1 a 6
const numeroUtente = prompt("Inserisci un numero da 1 a 6");

/* Number(numeroUtente); */

console.log(Number(numeroUtente));

const numeri = [1, 2, 3, 4, 5, 6];

for (i = 0; i < numeri.length; i++) {
    if 
}