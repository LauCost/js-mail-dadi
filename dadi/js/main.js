//Chiede all'utente di inserire un numero da 1 a 6
const numeroUtente = Number(prompt("Inserisci un numero da 1 a 6"));

/* Number(numeroUtente); */

console.log(numeroUtente);

const numeri = [1, 2, 3, 4, 5, 6];

console.log(numeri);


for (i = 0; i < numeri.length; i++) {
    if (numeroUtente != numeri[0]) {

    }
}